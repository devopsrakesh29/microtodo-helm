apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-gettask
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-gettask
  ports:
  - port: {{ .Values.gettask.servicePort }}
    targetPort: {{ .Values.gettask.port }}